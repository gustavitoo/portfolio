---
const skills = [
  {
    category: "Frontend",
    items: ["React", "Next.js", "TypeScript", "Tailwind CSS"],
  },
  {
    category: "Backend",
    items: ["Node.js", "PostgreSQL", "MongoDB", "APIs REST"],
  },
  { category: "Tools", items: ["Git", "Docker", "AWS", "Vercel"] },
  {
    category: "AI/ML",
    items: ["LLMs", "Prompt Engineering", "Vector DBs", "RAG"],
  },
];
---

<section
  id="about-section"
  class="py-20 px-4 sm:px-6 lg:px-8 border-t border-border"
>
  <div class="max-w-6xl mx-auto">
    <div class="space-y-12">
      {/* Title */}
      <div id="about-title" class="">
        <h2 class="text-4xl font-bold mb-4">Sobre mí</h2>
        <div class="w-12 h-1 bg-primary rounded-full"></div>
      </div>

      {/* Content */}
      <div id="about-text" class="grid md:grid-cols-2 gap-12">
        {/* Text */}
        <div class="space-y-4">
          <p class="text-muted-foreground leading-relaxed">
            Soy un apasionado desarrollador web con una mentalidad de
            emprendedor. Me encanta resolver problemas complejos y crear
            soluciones elegantes que hagan la diferencia.
          </p>
          <p class="text-muted-foreground leading-relaxed">
            Especializado en desarrollo Full-stack moderno, he trabajado con
            startups y empresas tech líderes, siempre enfocado en la calidad, el
            rendimiento y la experiencia del usuario.
          </p>
          <p class="text-muted-foreground leading-relaxed">
            Cuando no estoy codificando, me encontrarás explorando nuevas
            tecnologías, contribuyendo a proyectos open source o mentorando a
            otros desarrolladores.
          </p>
        </div>

        {/* Stats */}
        <div id="about-stats" class="space-y-6">
          <div
            class="bg-card border border-border rounded-lg p-6 hover:border-primary/50 transition-all"
          >
            <div class="text-3xl font-bold text-primary">20+</div>
            <p class="text-muted-foreground mt-2">Proyectos completados</p>
          </div>
          <div
            class="bg-card border border-border rounded-lg p-6 hover:border-primary/50 transition-all"
          >
            <div class="text-3xl font-bold text-primary">35+</div>
            <p class="text-muted-foreground mt-2">Clientes satisfechos</p>
          </div>
          <div
            class="bg-card border border-border rounded-lg p-6 hover:border-primary/50 transition-all"
          >
            <div class="text-3xl font-bold text-primary">6+</div>
            <p class="text-muted-foreground mt-2">Años de experiencia</p>
          </div>
        </div>
      </div>

      {/* Skills */}
      <div id="about-skills" class="mt-16">
        <h3 class="text-2xl font-bold mb-8">Habilidades</h3>
        <div class="grid sm:grid-cols-2 lg:grid-cols-4 gap-6">
          {
            skills.map((skillGroup) => (
              <div class="bg-card border border-border rounded-lg p-6 hover:border-primary/50 transition-all">
                <h4 class="font-semibold mb-4">{skillGroup.category}</h4>
                <div class="space-y-2">
                  {skillGroup.items.map((item) => (
                    <div class="text-sm text-muted-foreground hover:text-foreground transition-colors cursor-pointer">
                      ✓ {item}
                    </div>
                  ))}
                </div>
              </div>
            ))
          }
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  import { animate, inView } from "motion";

  inView("#about-section", (target) => {
    animate(target, { opacity: [0, 1], y: [0, 20] }, {
      duration: 0.8,
      easing: "ease-out",
    } as any);
  });

  inView("#about-title", (target) => {
    animate(target, { opacity: [0, 1], y: [0, 20] }, {
      duration: 0.8,
      easing: "ease-out",
    } as any);
  });

  inView("#about-text", (target) => {
    animate(target, { opacity: [0, 1], x: [-20, 0] }, {
      duration: 0.8,
      easing: "ease-out",
    } as any);
  });

  inView("#about-stats", (target) => {
    animate(target, { opacity: [0, 1], x: [40, 0] }, {
      duration: 0.8,
      easing: "ease-out",
    } as any);
  });

  inView("#about-skills", (target) => {
    animate(target, { opacity: [0, 1], y: [40, 0] }, {
      duration: 0.8,
      easing: "ease-out",
    } as any);
  });
</script>
