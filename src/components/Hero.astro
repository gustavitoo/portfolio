---

---

<section id="inicio" class="pt-32 pb-20 px-4 sm:px-6 lg:px-8 relative">
  <div class="max-w-6xl mx-auto">
    <div
      id="hero-container"
      class="text-center space-y-6 opacity-0 translate-y-5"
    >
      <div id="hero-avatar" class="flex justify-center opacity-0 scale-75">
        <div
          class="w-32 h-32 rounded-full bg-gradient-to-br from-primary/20 to-primary/5 border border-border flex items-center justify-center text-6xl"
        >
          👨‍💻
        </div>
      </div>

      <div id="hero-title" class="opacity-0">
        <h1 class="text-5xl sm:text-6xl font-bold tracking-tight">
          Hola, soy
          <span
            class="bg-linear-to-r from-foreground to-muted-foreground bg-clip-text text-transparent"
          >
            John Doe
          </span>
        </h1>
      </div>

      <p
        id="hero-text"
        class="opacity-0 text-lg sm:text-xl text-muted-foreground max-w-2xl mx-auto leading-relaxed"
      >
        Desarrollador Full-stack especializado en crear experiencias web
        modernas, rápidas y escalables. Con más de 5 años de experiencia
        construyendo productos digitales innovadores.
      </p>

      <div
        id="hero-buttons"
        class="flex gap-4 justify-center pt-4 opacity-0 translate-y-3"
      >
        <button
          class="px-8 py-3 bg-primary text-primary-foreground rounded-full font-medium hover:shadow-lg transition-all duration-300"
        >
          Ver mi trabajo
        </button>

        <button
          class="px-8 py-3 border border-border rounded-full font-medium hover:border-foreground transition-all duration-300"
        >
          Descargar CV
        </button>
      </div>
    </div>

    <div
      id="hero-floating"
      class="mt-16 relative h-64 rounded-xl bg-gradient-to-br from-primary/10 to-primary/5 border border-border overflow-hidden flex items-center justify-center"
    >
      <div class="text-center">
        <div class="text-6xl mb-4">🚀</div>
        <p class="text-muted-foreground">
          Listo para impulsar tu siguiente proyecto
        </p>
      </div>
    </div>
  </div>
</section>

<script>
  import { animate } from "motion";

  animate("#hero-container", { opacity: 1, y: 0 }, {
    duration: 0.8,
    easing: "ease-out",
  } as any);

  animate("#hero-avatar", { opacity: 1, scale: 1 }, {
    duration: 0.6,
    delay: 0.2,
    easing: "ease-out",
  } as any);

  animate("#hero-title", { opacity: 1 }, { duration: 0.8, delay: 0.3 } as any);

  animate("#hero-text", { opacity: 1 }, { duration: 0.8, delay: 0.4 } as any);

  animate(
    "#hero-buttons",
    { opacity: 1, y: 0 } as any,
    { duration: 0.8, delay: 0.5, easing: "ease-out" } as any,
  );

  animate(
    "#hero-floating",
    { y: [0, 20, 0] } as any,
    {
      duration: 4,
      easing: "ease-in-out",
      repeat: Infinity,
    } as any,
  );
</script>
